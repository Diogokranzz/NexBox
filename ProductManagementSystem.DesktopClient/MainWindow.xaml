<Window x:Class="ProductManagementSystem.DesktopClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="NexBox - Login" Height="520" Width="420"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Icon="pack://application:,,,/Assets/icon.png"
        MouseDown="Window_MouseDown">
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Border CornerRadius="14" Margin="12" Background="White">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.2" Color="#000"/>
        </Border.Effect>
        <Grid>
            <Grid Margin="40,30">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Button HorizontalAlignment="Right" VerticalAlignment="Top" Background="Transparent" BorderBrush="Transparent" Foreground="#94a3b8" Click="Close_Click" Padding="5" Cursor="Hand">
                    <md:PackIcon Kind="Close" Width="18" Height="18"/>
                </Button>

                <StackPanel Grid.Row="1" Margin="0,10,0,30" HorizontalAlignment="Center">
                    <Image Source="pack://application:,,,/Assets/icon.png" Width="80" Height="80" HorizontalAlignment="Center" Margin="0,0,0,16" RenderOptions.BitmapScalingMode="HighQuality"/>
                    <TextBlock Text="NexBox" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#1e293b"/>
                    <TextBlock Text="Sistema de Gestão de Produtos" FontSize="12" HorizontalAlignment="Center" Foreground="#94a3b8" Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="2">
                    <TextBlock Text="Entrar na sua conta" FontSize="16" FontWeight="SemiBold" Foreground="#1e293b" Margin="0,0,0,20"/>

                    <TextBox x:Name="txtUsername" md:HintAssist.Hint="Username" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Foreground="#1e293b" md:HintAssist.Foreground="#94a3b8" md:TextFieldAssist.UnderlineBrush="#e6570a" Margin="0,0,0,16" FontSize="14"/>
                    <PasswordBox x:Name="txtPassword" md:HintAssist.Hint="Password" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" Foreground="#1e293b" md:HintAssist.Foreground="#94a3b8" md:TextFieldAssist.UnderlineBrush="#e6570a" Margin="0,0,0,28" FontSize="14"/>

                    <Button x:Name="btnLogin" Content="ENTRAR" Click="BtnLogin_Click" Height="44" Foreground="White" Background="#e6570a" BorderThickness="0" FontWeight="Bold" FontSize="14" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="0">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#cc4a08"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <TextBlock x:Name="txtError" Foreground="#dc2626" Margin="0,12,0,0" HorizontalAlignment="Center" TextWrapping="Wrap" FontSize="12"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
