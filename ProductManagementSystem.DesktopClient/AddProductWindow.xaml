<Window x:Class="ProductManagementSystem.DesktopClient.AddProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="NexBox - Novo Produto" Height="500" Width="400"
        WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Icon="pack://application:,,,/Assets/icon.png"
        MouseDown="Window_MouseDown">
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Border CornerRadius="12" Margin="15" Background="White">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.15" Color="#000"/>
        </Border.Effect>
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid>
                <TextBlock x:Name="txtTitle" Text="Adicionar Novo Produto" FontSize="18" FontWeight="Bold" Foreground="#1e293b"/>
                <Button HorizontalAlignment="Right" VerticalAlignment="Top" Background="Transparent" BorderBrush="Transparent" Foreground="#94a3b8" Click="Close_Click" Padding="0" Height="24" Width="24" Cursor="Hand">
                    <md:PackIcon Kind="Close"/>
                </Button>
            </Grid>

            <StackPanel Grid.Row="1" Margin="0,20,0,0">
                <TextBox x:Name="txtNome" md:HintAssist.Hint="Nome do Produto" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Foreground="#1e293b" md:HintAssist.Foreground="#94a3b8" md:TextFieldAssist.UnderlineBrush="#e6570a" Margin="0,0,0,20" FontSize="14"/>
                <TextBox x:Name="txtPreco" md:HintAssist.Hint="Preço (R$)" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Foreground="#1e293b" md:HintAssist.Foreground="#94a3b8" md:TextFieldAssist.UnderlineBrush="#e6570a" Margin="0,0,0,20" FontSize="14"/>
                <TextBox x:Name="txtEstoque" md:HintAssist.Hint="Unidades em Estoque" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Foreground="#1e293b" md:HintAssist.Foreground="#94a3b8" md:TextFieldAssist.UnderlineBrush="#e6570a" Margin="0,0,0,10" FontSize="14"/>
                <TextBlock x:Name="txtError" Foreground="#dc2626" TextWrapping="Wrap" HorizontalAlignment="Center" FontSize="12"/>
            </StackPanel>

            <Button Grid.Row="2" x:Name="btnSave" Content="SALVAR PRODUTO" Click="BtnSave_Click" Height="44" Foreground="White" Background="#e6570a" BorderThickness="0" FontWeight="Bold" FontSize="13" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#cc4a08"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</Window>
